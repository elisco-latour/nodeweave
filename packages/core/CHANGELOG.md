# @build744/nodeweave-core

## 0.2.0

### Minor Changes

- Release 0.2.0 — first release from the standalone package repository. The app
  application has been extracted to its own repository and removed from this repo's
  history, leaving a focused engine + renderers + authoring toolkit. The release
  pipeline is finalized: tokenless npm Trusted Publishing (OIDC) and a hardened
  `npm pack --json` dry-run gate.
